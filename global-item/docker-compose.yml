version: '3.8'

services:
  mongo:
    image: mongo:latest
    container_name: global-item
    ports:
      - "27020:27017"
    volumes:
      - mongo-data:/data/db

  # mongo-express:
  #   image: mongo-express:latest
  #   container_name: mongo-express
  #   restart: always
  #   ports:
  #     - "8082:8081"
  #   environment:
  #     ME_CONFIG_MONGODB_SERVER: mongodb # The name of the MongoDB service
  #     ME_CONFIG_MONGODB_PORT: 27017
  #     ME_CONFIG_MONGODB_ADMINUSERNAME: root
  #     ME_CONFIG_MONGODB_ADMINPASSWORD: example

volumes:
  mongo-data:



